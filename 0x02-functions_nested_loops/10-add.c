#include "main.h"
/**
 * add - adds two integers and returns the result
 *
 * @x: number being added
 *
 * @y: number being added
 *
 * Reutrn: Always 0
 *
 */

int add(int x, int y)
{
	return (x + y);
}
